import { useState, useEffect } from 'react';
import { getTrailPolyline } from '../utils/getTrailPolyline';

export function useTrailLocation() {
    const [coords, setCoords] = useState(undefined);

    function getTrailPolylineFromName()

    return {coords, }
}
